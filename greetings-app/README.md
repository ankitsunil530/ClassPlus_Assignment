# Custom Greetings & Wishes Frontend

React + Vite + Tailwind CSS frontend for the Custom Greetings & Wishes App.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Create `.env` from `.env.example` and set:

```text
VITE_API_BASE_URL=http://localhost:5000/api
```
